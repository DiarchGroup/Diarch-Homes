# 301s from the old WordPress URLs so their rankings carry over.
# Confirm the full list in Google Search Console → Pages and add any missing ones.
# Netlify format. On Vercel use vercel.json "redirects"; on Apache use .htaccess.
/about-us        /about      301!
/contact-us      /contact    301!
/our-services    /projects   301!
/services        /projects   301!
/home            /           301!

# Known routes are prerendered as real files. Unmatched paths must remain 404s.
